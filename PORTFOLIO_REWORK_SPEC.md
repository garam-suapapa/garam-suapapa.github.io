# 포트폴리오 전체 개편 작업 지침

작성일: 2026-09-10 (한국 시간)
대상 작업 폴더: `F:\portpolio`
실행 모델: 사용자가 선택한 GPT-5.6 Sol (`gpt-5.6-sol`)
문서 상태: 구현 인계용 사양. 이 문서를 만든 시점에는 전체 개편을 실행하지 않았다.

## 1. 목표와 작업 범위

기존 게임 기획 포트폴리오의 6개 프로젝트를, 기획 판단과 실제 산출물을 읽기 쉽게 설명하는 사례 중심 구성으로 개편한다. 디자인·문구 구성 기준은 `stamina-case-preview.html` 오른쪽 개선안이다. 비교 화면의 왼쪽 BEFORE 영역은 정식 상세 페이지에 넣지 않는다.

완료 범위는 메인 페이지, 프로젝트 상세 페이지 6개, 기존 작업 산출물 페이지의 설명·탐색 개선, PC·모바일 확인이다. 계획 제시나 대표 사례 한 개 구현에서 멈추지 않는다. 실제 업무 사실이 부족한 부분은 추측하지 말고 근거가 있는 내용으로 페이지를 완성한 뒤 확인 항목을 별도 기록한다.

이번 인계는 로컬 개편과 검증까지다. Git 커밋·푸시·외부 배포·사용량 초기화권 사용은 이 문서로 지시하지 않는다. 사용자가 별도로 요청하면 해당 범위를 수행한다.

## 2. 먼저 읽을 자료

1. 현재 작업 경로의 적용 가능한 `AGENTS.md`와 `git status --short`.
2. `stamina-case-preview.html`: 디자인, 문장 길이, 정보 위계, 표·이미지 해설의 참고용.
3. `index.html`: 프로필, 경력, 대표 프로젝트, 도구명과 기존 사용자 수정 내용.
4. `script.js`: `projectData`의 6개 프로젝트 설명 및 이미지 연결.
5. `style.css`, `legendofheroes.html`, `moonlight.html`, `warofcrown.html`, `portfolio.test.js`.
6. 사례별로 연결된 `assets` 이미지. 사용할 정확한 파일을 열어 실제로 읽히는 내용만 설명한다.

인계 시점의 기존 변경: `index.html`, `portfolio.test.js`가 수정 상태이며 `stamina-case-preview.html`, `추천서_FPS게임기획_수정안.md`는 미추적 상태다. 시작할 때 상태를 다시 확인한다. 사용자 변경을 되돌리거나 HEAD 내용으로 덮어쓰지 않는다.

## 3. 사실과 예시를 구분하는 작성 규칙

- 기존 포트폴리오 문구는 사용자가 제공한 경력 설명이다. 새로운 경력·직급·성과·담당 업무를 만들지 않는다.
- 기존 문구 안의 인과관계나 개선 성과도 독립적으로 검증된 사실이라고 간주하지 않는다. 관찰 근거가 없으면 목표·의도로 정리한다.
- `stamina-case-preview.html`의 자동 충전분 우선 차감, 합산 입장, 입장 비용 10, 중복 요청 처리, 담당 범위, 테스트 계획은 가상 예시다. 실제 사례로 복사하지 않는다.
- 스태미너 원본 화면에는 ‘충전 스태미너’와 ‘예비 스태미너’가 표시된다. 이를 임의로 ‘자동/유료’와 동일시하지 않는다. 기존 설명과 화면의 용어가 다른 점은 확인 항목에 남긴다.
- 역할·기간·회사·연락처·상용화 이력은 기존 자료를 유지한다. 서로 충돌하면 임의로 하나를 확정하지 않는다.
- 유지할 간결한 도구명: `OA`, `Unity`, `Notion`, `AI`. AI 활용 방식이나 실무 성과를 새로 만들지 않는다.
- ‘이탈 감소’, ‘참여율 향상’, ‘결제 전환율 향상’, ‘Retention 개선’은 측정 자료가 없으면 확인된 결과처럼 쓰지 않는다. ‘참여 유도’, ‘진입 부담 완화를 목표로 설계’처럼 의도를 정확히 표현한다.
- 수치가 없다는 이유로 모든 사례를 삭제하지 않는다. 실제 규칙·화면·문서와 담당 내용을 중심으로 설명한다.
- 비교 시안처럼 가상의 설명이 필요한 경우 정식 경력 본문에 섞지 않는다. 기본 구현에는 새로운 가상 사례를 추가하지 않는다.
- 모르는 내용은 `PORTFOLIO_CONTENT_CHECKS.md`에 프로젝트, 원문, 근거 경로, 확인할 질문, 현재 처리 방식으로 모은다. 공통 확인 사항을 묶어 사용자에게 전달한다.
- 공개 화면에 빈 칸, 대괄호 자리표시자, 장문의 ‘확인 필요’ 메모, 작업자 지시문을 노출하지 않는다. 근거가 없는 섹션은 자연스럽게 생략하거나 다른 섹션과 합친다.

## 4. 프로젝트별 적용 범위

프로젝트별 상세 사례는 아래를 기본 후보로 삼는다. 실제 이미지·문서가 더 충실한 사례가 있으면 같은 프로젝트 안에서 교체하고 이유를 기록한다. 모든 기존 담당 업무와 자료 링크는 상세 사례 또는 ‘그 밖의 담당 업무’에 남긴다.

| 프로젝트 키 | 프로젝트 | 상세 사례 우선 후보 | 분량 기준 |
| --- | --- | --- | --- |
| legend | 영웅전설:가가브트릴로지 | 스태미너 충전 구조 / 추천 팀 편성 | 핵심 사례 최대 2개 + 나머지 업무 요약 |
| moonlight | 달빛조각사 & 다크게이머 | 이벤트 미션·보상 구조 / 웨이브 보스전 | 핵심 사례 최대 2개 + 나머지 업무 요약 |
| madworld | MADWORLD | BT 기반 보스 패턴 / 인터랙션 오브젝트 | 핵심 사례 최대 2개 + 나머지 업무 요약 |
| warofcrown | WAROFCROWN | 난이도 선택형 일일 던전 또는 랭킹 레이드 | 핵심 사례 1개 + 나머지 업무 요약 |
| slashheavens | 천지를베다 | 전용 장비·초월 성장 구조 | 핵심 사례 1개 + 나머지 업무 요약 |
| guardians | 가디언즈 | 속성별 보스와 친구 캐릭터 사용 | 핵심 사례 1개 + 나머지 업무 요약 |

후보 수를 맞추기 위해 내용을 창작하지 않는다. 근거가 부족하면 대표 프로젝트도 사례 1개로 압축하고 나머지는 업무 요약으로 보존한다. 달빛조각사와 다크게이머의 개별 업무 귀속이 불명확하면 두 프로젝트 공통 경험처럼 재해석하지 말고 원래 묶음 표기를 유지한다.

## 5. 상세 페이지 구성

### 페이지 상단

- 프로젝트 이름, 기존 담당 역할, 기간, 장르.
- 담당 범위를 요약한 1~2문장. 실제 기획한 대상이 드러나게 쓴다.
- 사례 목차와 ‘포트폴리오로 돌아가기’, 해당 프로젝트의 산출물 링크.
- 메인 프로필과 경력 전체를 상세 페이지에 반복하지 않는다.

### 핵심 사례

다음 순서를 공통으로 사용하되 근거가 부족한 항목은 합치거나 생략한다. 상세 사례 한 개의 설명은 대략 700~1,300자 수준을 우선하고 자료량에 맞춰 조정한다. 길이를 채우기 위한 일반론은 쓰지 않는다.

1. **문제와 목표:** 어떤 플레이 상황에서 무엇이 문제였는지, 어떤 경험을 만들려 했는지.
2. **설계 기준:** 실제 설명과 자료에서 드러나는 우선 조건. 비교하지 않은 대안을 비교했다고 쓰지 않는다.
3. **핵심 규칙과 화면:** 기능명 대신 동작 관계를 설명한다. 규칙 비교가 유용할 때 짧은 표나 CSS 흐름도를 사용한다.
4. **예외 처리:** 문서나 사용자 설명에 있는 실제 케이스만 담는다. 게임 기획 일반론으로 빈 항목을 채우지 않는다.
5. **담당 범위와 협업:** 직접 설계·문서화·검토한 범위를 기존 근거 안에서 기술한다. 메인·리드 직함만으로 팀 인원, 승인 권한, QA·출시 책임을 추정하지 않는다.
6. **검증 또는 반영 내용:** 실제 테스트 기록·반영 범위·피드백이 있을 때 제시한다. 기대 효과를 검증 결과로 바꾸지 않는다. 기록이 없으면 관련 목표 설명과 통합한다.

### 이미지와 산출물

- 실제 기존 이미지 원본을 그대로 사용한다. 재생성·덧그리기·문구 합성·원본 덮어쓰기를 하지 않는다.
- 그림마다 ‘자료명 / 보아야 할 부분 / 해당 사례와의 연결’을 1~3문장으로 붙인다.
- 기획서 이미지 안의 표·그래프는 직접 확인한 내용만 설명한다. 화면만으로 내부 구현·차감 우선순위·담당자를 추정하지 않는다.
- 원본 확대 링크를 제공하고 새 탭 열림을 알린다. 이미지 원본 페이지는 브라우저 확대가 가능해야 한다.
- 의미 있는 `alt`, 정확한 `width`·`height`, 화면 아래 이미지의 `loading="lazy"`를 적용한다.

## 6. 디자인과 메인 페이지

디자인은 비교 시안 오른쪽의 밝은 바탕, 짙은 글자, 녹색 강조, 넓은 본문, 절제된 번호·표·구분선을 기준으로 한다. 커다란 제목과 설명이 첫 화면을 전부 차지하지 않도록 한다.

- 메인은 프로필 요약 → 대표 프로젝트 3개 → 경력 → 실무 역량 → 전체 프로젝트 → 연락처 흐름으로 정돈한다. 경력 상세는 유지하되 대표 사례 접근을 앞당긴다.
- 프로필의 이름·10년 경력·전문 장르·연락처를 보존하고 반복되는 경력 소개만 압축한다. ‘Top 5’의 순위 종류·시점이 불명확하면 확대 해석하지 말고 확인 항목에 기록한다.
- 상단 메뉴에 대표 프로젝트, 경력, 연락처 앵커를 제공한다. 고정 메뉴가 제목을 가리지 않도록 스크롤 여백을 둔다.
- 대표 카드에는 역할·기간·구체적인 기획 대상·상세 사례 링크를 넣는다. 전체 프로젝트는 간결한 목록/카드로 유지해 같은 설명을 반복하지 않는다.
- 상세 페이지는 긴 본문을 작은 모달에 가두지 않는다. PC 본문 최대 폭 약 820~900px, 본문 16px 전후, 행간 1.75~1.9를 기준으로 조정한다.
- 모바일에서는 1열, 가로 넘침 없음, 읽기 쉬운 이미지·표·터치 영역을 보장한다.
- 기존 테마 전환·연락처 복사·프로젝트 필터 기능을 유지한다. 상세·산출물 페이지도 선택한 테마를 유지하고 밝은/어두운 테마 모두 본문 대비를 확인한다.
- OA·Unity·Notion·AI의 기존 활용 설명은 보존한다. 관련 산출물로 연결할 근거가 있으면 링크를 추가한다. 숙련도 막대를 유지할 경우 새 평가 점수는 부여하지 않는다.
- 키보드 접근, 보이는 포커스, 제목 순서, 동작 줄이기 설정을 고려한다. 핵심 정보가 hover에만 나타나지 않게 한다.
- 이번 범위에 새로운 프레임워크, 빌드 시스템, 로그인, 서버 API, 분석 추적, 이미지 생성, PDF 제작은 추가하지 않는다.

## 7. 파일과 연결 사양

정적 HTML/CSS/JavaScript 구성을 유지한다. 프로젝트 상세 경로는 아래 6개로 통일한다.

- `cases/legend.html`
- `cases/moonlight.html`
- `cases/madworld.html`
- `cases/warofcrown.html`
- `cases/slashheavens.html`
- `cases/guardians.html`

필요하면 `case-study.css`와 `case-study.js`를 공통 파일로 추가한다. 본문은 각 HTML에 작성해 JavaScript가 없어도 읽히게 한다. 공통 테마·탐색 코드는 재사용하며 6개 페이지에 중복 구현하지 않는다.

- `index.html`의 프로젝트 카드 클릭은 해당 상세 페이지로 이동하는 실제 링크로 전환한다.
- 기존 모달 전용 코드는 새 링크와 충돌하지 않게 제거·정리한다. 테마·복사·필터 동작까지 함께 삭제하지 않는다.
- 상세 문구는 새 페이지 한 곳에서 관리한다. `script.js`에 동일 장문을 복사해 두지 않는다.
- `legendofheroes.html`, `moonlight.html`, `warofcrown.html`의 기존 주소와 자료를 보존한다. 자료 해설과 관련 사례로 돌아가는 링크를 보강한다.
- `warofcrown.html`은 현재 파일이 있지만 `projectData.warofcrown.link`는 null이다. 실제 자료가 해당 사례와 맞는지 확인한 뒤 연결한다.
- 중첩된 `cases/`에서 원본 이미지와 공통 CSS/JS는 `../assets/...`, `../style.css` 등 상대 경로를 쓴다. GitHub Pages 하위 경로에서도 동작하게 루트 절대 경로를 피한다.
- `stamina-case-preview.html`은 비교 기록으로 보존하고 메인 탐색에 정식 사례로 연결하지 않는다.
- `cover_letter.md`, 추천서, 다른 프로젝트 폴더는 수정 대상에서 제외한다.

## 8. 작업 순서와 사용량 관리

1. 관련 파일만 읽고 기존 테스트 결과를 한 번 기록한다. 전체 저장소·대화·이미지를 매번 다시 읽지 않는다.
2. 6개 프로젝트의 기존 문구·연결 이미지·확인 필요 내용을 짧게 매핑한다. 실제 작업 내용이 확인되지 않은 부분이 있어도 공통 레이아웃과 다른 사례는 계속 진행한다.
3. 영웅전설 상세 페이지와 공통 스타일을 먼저 만든다. 임의의 시안 내용을 사실처럼 쓰지 않는다.
4. 그 페이지를 PC·모바일에서 확인하고 공통 레이아웃을 확정한다. 사용자 재승인을 기본 단계로 추가하지 않는다.
5. 같은 구성을 나머지 5개 프로젝트에 적용한다. 프로젝트별 디자인을 새로 탐색하지 않는다.
6. 메인 카드, 산출물 페이지, 공통 기능을 연결한다.
7. 전체 링크·이미지·키보드·반응형·테마를 확인하고 수정한다.
8. 완료 파일, 실행 주소, 확인 결과와 내용 확인 질문을 간결하게 보고한다.

Sol로 순차 수행한다. 별도 요청 없이 하위 에이전트나 새 작업을 생성하지 않는다. 웹 검색은 현재 프로젝트를 읽는 데 필수적이지 않다. 외부 사실 검증이 필요하면 해당 항목만 좁게 검색한다. 새 자료 조사, 과도한 디자인 반복, 모든 장면의 캡처 산출물은 추가하지 않는다.

모델 전환은 실행 환경 설정이다. 이 파일을 읽는 것만으로 모델이 바뀌지는 않는다. 현재 실행 모델을 바꿨다고 주장하지 않는다. 높은 추론 설정이 필요한 오류가 없으면 사용자가 선택한 일반 설정으로 진행한다.

## 9. 검증과 완료 기준

인계 시점 관찰: 기존 `node --test portfolio.test.js`는 8개 통과, `HongGaram_Portfolio.html` 누락으로 1개 실패했다. 이 상태는 시작 시 다시 확인한다. 테스트 통과를 위해 무관한 파일을 조용히 생성하거나 실패 테스트를 지우지 않는다. 새 구조로 달라진 카드/모달 테스트는 새로운 동작을 검사하도록 수정한다. 기존 제출 파일 문제가 범위 밖으로 남으면 별도 보고한다.

필수 확인:

- [ ] 메인에서 6개 프로젝트 모두 상세 페이지에 접근 가능.
- [ ] 각 상세 페이지 주소로 직접 접속 가능, 뒤로가기·메인 복귀 정상.
- [ ] 기존 담당 업무와 이미지·산출물이 누락되지 않음.
- [ ] 대표 사례가 실제 문제·규칙·담당 내용 중심으로 읽힘.
- [ ] 가상 규칙·수치·역할·성과가 실제 경력에 섞이지 않음.
- [ ] 이미지 캡션이 실제 보이는 내용과 일치하고 원본 확대 가능.
- [ ] 모든 내부 링크·이미지·CSS·JS 경로 유효.
- [ ] 메인·상세·산출물 사이 테마 유지, 연락처 복사와 필터 정상.
- [ ] JavaScript 비활성 상태에서도 상세 본문 접근 가능.
- [ ] PC 약 1280px, 모바일 약 390px에서 주요 화면 육안 확인 및 가로 넘침 검사.
- [ ] 키보드 탐색과 포커스 표시 정상. 모달을 남기는 경우 Escape·포커스 반환 확인.
- [ ] 변경한 JavaScript 구문 검사, 관련 테스트, 변경 범위 공백 검사 실행.
- [ ] 작업 전부터 있던 실패와 새 회귀를 구분하여 보고.
- [ ] `PORTFOLIO_CONTENT_CHECKS.md`에 남은 사실 확인 항목 정리.

검사는 `node --check script.js`, 추가 JS가 있으면 해당 파일 구문 검사, `node --test portfolio.test.js`, `git diff --check`를 사용한다. 새 페이지 경로·연결, 필터, 테마 유지처럼 실제 실패 가능성이 있는 동작을 검증한다. 문장 전체의 정규식 복제 테스트나 반복적인 전체 테스트 실행은 피한다.

로컬 서버는 기존 `http://127.0.0.1:48173/`가 이 작업 폴더를 제공하는지 먼저 확인한다. 종료되어 있으면 작업 폴더에서 `python -m http.server 48173 --bind 127.0.0.1`로 실행한다. 포트가 다른 용도로 사용 중이면 해당 프로세스를 종료하지 말고 빈 포트를 사용한다.

최종 보고에서는 로컬 검토 완료와 외부 배포를 구분한다. 사용자에게 메인 개편 페이지를 열어 주고 변경 요점, 테스트 결과, 내용 확인 질문만 간결하게 전달한다.

## 10. Sol에 전달할 시작 문장

> `F:\portpolio\PORTFOLIO_REWORK_SPEC.md`를 읽고 포트폴리오 전체 개편을 실행해줘. `stamina-case-preview.html` 오른쪽 구성을 참고해 메인과 6개 프로젝트 상세 페이지, 산출물 탐색을 정리해줘. 기존 문구와 이미지에서 확인되는 사실만 사용하고, 가상 규칙·성과·역할은 옮기지 마. 확인이 필요한 내용은 별도 파일에 모으되 가능한 구현은 계속해서 로컬 브라우저 검증까지 완료해줘. 기존 사용자 변경을 보존하고, 커밋·푸시·외부 배포·사용량 초기화권 사용은 하지 마.
